app:
  keycloak:
    admin:
      clientId: admin-cli
      clientSecret: 3bTUBnovzrfjVnaEaSkNfGNiatEeixeB
    realm: education-app
    serverUrl: http://localhost:9082

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${app.keycloak.serverUrl}/realms/${app.keycloak.realm}
